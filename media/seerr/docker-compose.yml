services:
    seerr:
       image: ghcr.io/seerr-team/seerr:latest
       container_name: seerr
       environment:
            - LOG_LEVEL=debug
            - TZ=Asia/Tashkent
       networks: 
            - frontend
       #ports:
            #- 5055:5055
       volumes:
            - /home/duckdarsh/home-lab/appdata/media/seerr/config:/app/config
       restart: unless-stopped
networks:
  frontend:
    external: true
